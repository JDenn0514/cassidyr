% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/context-file.R
\name{cassidy_describe_file}
\alias{cassidy_describe_file}
\title{Read File Contents as Context}
\usage{
cassidy_describe_file(
  path,
  max_lines = Inf,
  lines = NULL,
  line_range = NULL,
  show_line_numbers = TRUE,
  level = c("full", "summary", "index")
)
}
\arguments{
\item{path}{Path to the file to read. Can be absolute or relative to the
working directory.}

\item{max_lines}{Maximum number of lines to read. Use \code{Inf} to read entire
file. Defaults to \code{Inf} (read all).}

\item{lines}{Numeric vector specifying specific line numbers to read.
If provided, only these lines are included. Use \code{NULL} (default) to read
all lines (subject to \code{max_lines}).}

\item{line_range}{Numeric vector of length 2 specifying start and end lines
(e.g., \code{c(10, 50)} reads lines 10-50). Ignored if \code{lines} is provided.}

\item{show_line_numbers}{Logical; if \code{TRUE}, prepends line numbers to each
line. Useful for discussing specific line numbers. Defaults to \code{TRUE}.}

\item{level}{Character; detail level for file content. One of:
\itemize{
\item \code{"full"}: Complete file contents (default)
\item \code{"summary"}: Function signatures + key excerpts
\item \code{"index"}: Metadata and function listing only
Used by chat system to manage context size. Most users should use default.
}}
}
\value{
A \code{cassidy_context} object containing the formatted file contents.
}
\description{
Reads a file and formats it as context for use with \code{\link[=cassidy_chat]{cassidy_chat()}}.
The file content is wrapped in a markdown code block with appropriate
syntax highlighting based on file extension.
}
\details{
The function automatically detects the file type from the extension and
applies appropriate syntax highlighting in the markdown output.

You can read files in three ways:
\itemize{
\item Entire file: Just provide \code{path}
\item Specific lines: Use \code{lines = c(1, 5, 10:20)}
\item Line range: Use \code{line_range = c(10, 50)}
}

The \code{level} parameter controls detail:
\itemize{
\item \code{"full"}: Best for detailed code review, includes all content
\item \code{"summary"}: Good for understanding structure without full content
\item \code{"index"}: Minimal metadata, useful when you have many files
}
}
\examples{
\dontrun{
  # Read an entire R file
  ctx <- cassidy_describe_file("R/my-function.R")

  # Read specific lines (useful for focusing on a function)
  ctx <- cassidy_describe_file("R/my-function.R", lines = 45:120)

  # Read with summary level (less detail)
  ctx <- cassidy_describe_file("R/my-function.R", level = "summary")

  # Ask Cassidy to review specific code
  cassidy_chat(
    "Review this function and suggest improvements",
    context = cassidy_describe_file("R/my-function.R", lines = 45:120)
  )
}

}
\seealso{
\code{\link[=cassidy_context_combined]{cassidy_context_combined()}}, \code{\link[=cassidy_context_project]{cassidy_context_project()}},
\code{\link[=cassidy_chat]{cassidy_chat()}}
}
