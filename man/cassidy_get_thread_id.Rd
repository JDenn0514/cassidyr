% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chat-persistence.R
\name{cassidy_get_thread_id}
\alias{cassidy_get_thread_id}
\title{Get thread ID from conversation ID}
\usage{
cassidy_get_thread_id(conv_id)
}
\arguments{
\item{conv_id}{Character. The \strong{local conversation ID} from
\code{\link[=cassidy_list_conversations]{cassidy_list_conversations()}} (e.g., "conv_20260131_1234").}
}
\value{
Character. The Cassidy API thread_id, or NULL if the conversation
has no thread_id. Returns NULL with a warning if the conversation doesn't
exist.
}
\description{
Retrieves the Cassidy API thread_id associated with a saved conversation.
This is the bridge between your locally saved conversations and the Cassidy
API functions.
}
\details{
\subsection{Why You Need This Function}{

The cassidyr package uses two different ID systems:
\enumerate{
\item \strong{Conversation IDs} (local): Generated by the cassidyr app when you save
conversations. Format: \code{"conv_YYYYMMDD_HHMMSS_RAND"}. Used with:
\itemize{
\item \code{\link[=cassidy_export_conversation]{cassidy_export_conversation()}}
\item \code{\link[=cassidy_delete_conversation]{cassidy_delete_conversation()}}
\item \code{\link[=cassidy_app]{cassidy_app()}} (for resuming)
}
\item \strong{Thread IDs} (API): Generated by Cassidy's servers when you create a
thread. Format: UUID-like string from Cassidy. Used with:
\itemize{
\item \code{\link[=cassidy_get_thread]{cassidy_get_thread()}}
\item \code{\link[=cassidy_send_message]{cassidy_send_message()}}
\item All other API functions
}
}

This function converts from local conversation IDs to API thread IDs.
}
}
\examples{
\dontrun{
  # List your saved conversations
  convs <- cassidy_list_conversations()
  print(convs)  # Shows both id and thread_id columns

  # Get thread_id from conversation_id
  conv_id <- convs$id[1]  # e.g., "conv_20260131_1234"
  thread_id <- cassidy_get_thread_id(conv_id)

  # Now use it with API functions
  if (!is.null(thread_id)) {
    thread <- cassidy_get_thread(thread_id)
    print(thread)
  }

  # Or skip the conversion - thread_id is in the table!
  thread_id <- convs$thread_id[1]
  thread <- cassidy_get_thread(thread_id)
}

}
\seealso{
\itemize{
\item \code{\link[=cassidy_list_conversations]{cassidy_list_conversations()}} to see both IDs side-by-side
\item \code{\link[=cassidy_get_thread]{cassidy_get_thread()}} to retrieve thread history from the API
}

Other chat-app: 
\code{\link{cassidy_app}()},
\code{\link{cassidy_delete_conversation}()},
\code{\link{cassidy_export_conversation}()},
\code{\link{cassidy_list_conversations}()}
}
\concept{chat-app}
