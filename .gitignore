# R
.Rproj.user
.Rhistory
.RData
.Ruserdata

# OAuth credentials (if added later)
.httr-oauth
.secrets

# Never commit real API keys
.Renviron
.env

# Optional: Ignore local test mocks (if you want to keep real ones locally)
# tests/testthat/app.cassidyai.com/
# But DO commit the redacted ones for CI/CD
docs
