# Render single file item with proper indentation

Render single file item with proper indentation

## Usage

``` r
.render_file_item_nested(
  file,
  is_selected,
  is_sent,
  needs_refresh = FALSE,
  level
)
```
