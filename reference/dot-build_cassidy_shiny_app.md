# Build the Cassidy Shiny app object

Build the Cassidy Shiny app object

## Usage

``` r
.build_cassidy_shiny_app(
  theme,
  context_level,
  context_text,
  assistant_id,
  api_key,
  timeout
)
```
