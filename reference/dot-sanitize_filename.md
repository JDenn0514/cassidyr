# Sanitize a string for use as a filename

Removes or replaces characters that are invalid in filenames.

## Usage

``` r
.sanitize_filename(x)
```

## Arguments

- x:

  Character. String to sanitize.

## Value

Character. Safe filename string.
