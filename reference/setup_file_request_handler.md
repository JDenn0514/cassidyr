# Setup file request confirmation handler

Setup file request confirmation handler

## Usage

``` r
setup_file_request_handler(
  input,
  session,
  conv_manager,
  assistant_id,
  api_key,
  timeout
)
```
