# Setup handler for confirming new chat with context

Setup handler for confirming new chat with context

## Usage

``` r
setup_new_chat_confirm_handler(
  input,
  session,
  conv_manager,
  assistant_id,
  api_key,
  timeout
)
```
