# Gather context (unified function)

Gather context (unified function)

## Usage

``` r
gather_context(
  config = TRUE,
  session = TRUE,
  git = FALSE,
  data = TRUE,
  data_method = "basic",
  files = NULL,
  data_frames = NULL,
  skills = NULL
)
```
